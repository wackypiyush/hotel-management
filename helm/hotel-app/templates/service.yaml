apiVersion: v1
kind: Service
metadata:
  name: {{ include "hotel-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "hotel-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
